# Training Service (DEPRECATED)

**Main idea**: Service that only trains given model with given experience, does 1 optimization step and returns model.

**Problem**: Sending torch model with HTTPS was problematic.

**Current status**: Not used. All training is inside INF service.
