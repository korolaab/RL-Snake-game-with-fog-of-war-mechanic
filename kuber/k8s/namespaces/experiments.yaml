apiVersion: v1
kind: Namespace
metadata:
  name: experiments
  labels:
    name: experiments
    purpose: rl-ml-workloads
    environment: dynamic
  annotations:
    description: "RL training jobs and ML experiments"
